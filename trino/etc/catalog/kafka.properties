connector.name=kafka
kafka.table-description-dir=/etc/trino/catalog/kafka
kafka.nodes=pkc-921jm.us-east-2.aws.confluent.cloud:9092
kafka.config.resources=/etc/trino/kafka-config/kafka-configuration.properties
kafka.table-names=stock_ticks,candle_1minute,candle_10minute,candle_1hour,candle_1day

#kafka.message-format=json
kafka.hide-internal-columns=false


kafka.default-schema=default

# Security configuration for Confluent Platform
#kafka.security-protocol=SASL_SSL
#kafka.sasl.mechanism=PLAIN
#kafka.sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username='PHDKSCROMJ3NUVMK' password='7wsw6Q9kisiXrt8PlK3WUCJZ9LGv41av01p8cnTbQ9doliCV4gdw1k52WzBsR5Y8';

# SSL configuration (if using Confluent Cloud or SSL)
#kafka.ssl.endpoint.identification.algorithm=https
#kafka.ssl.truststore.location=/etc/trino/kafka/kafka.truststore.jks
#kafka.ssl.truststore.password=<truststore-password>

# Performance tuning
#kafka.fetch.min.bytes=1024
#kafka.fetch.max.wait.ms=500
#kafka.max.poll.records=1000
#kafka.session.timeout.ms=30000

# Schema Registry configuration (for Avro, Protobuf, or JSON Schema)
#kafka.schema-registry-url=https://schema-registry:8081
#kafka.schema-registry-auth=<schema-registry-api-key>:<schema-registry-api-secret>

# Message decoding configuration
